import 'package:flutter/material.dart';

class TemporalScreen extends StatelessWidget {
  const TemporalScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text("Temporal Screen"),
      ),
    );
  }
}
